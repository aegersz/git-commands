git checkout -b reason

git add file1
git add dir/file2

#git rm file1
# But if you want to remove the file only from the Git repository and not remove it from the filesystem, use:
# git rm --cached file1

git commit -m new
git diff
git checkout master
git merge new
git remote add origin https://github.com/aegersz/yourrepo.git
git push -u origin master
git status
git log

